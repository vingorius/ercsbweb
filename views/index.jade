extends layout/basiclayout

block content
    div.container
        if (!user)
          a(href="/manager/login") Login
          br
          a(href="/manager/register") Register
        if (user)
          p You are currently logged in as #{user.username}
          a(href="/manager/logout") Logout
